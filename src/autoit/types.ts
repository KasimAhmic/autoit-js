// TODO: Figure out if we want to leave these as TypeScript types or if we want to make them concrete objects
// using some feature of ffi-rs. Not clue if that's even possible.

export type TLPCWSTR = string;
export type TLPWSTR = string;
export type THWND = number;
export type TDWORD = number;
export type TLPRECT = number;
export type TLPPOINT = number;
